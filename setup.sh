# TODO create a script that creates a fonts/ folder, and images/ images/input/ images/output/ folders.

# TODO create a separate script that (after above) contact chatGPT api to:
#   1. Generate list of 12 per month in json format {image, month, name, rating, description}
#   2. Save the json response in `list.json`
#   3. Generate images for those 12 in portrait mode using some effecct
#   4. Download images and store them in `images/input/`.
#   5. Run main.py
